package com.example.monopoly_deal_game.network;

import com.example.monopoly_deal_game.logic.PlayerCommand;

/**
 * 客户端（设计图 NetworkClient）。
 *
 * TODO(network): 发送 {@link PlayerCommand}；接收会话快照更新本地展示。
 */
public class NetworkClient {

    public void send(PlayerCommand command) {
        throw new UnsupportedOperationException("TODO(network): send");
    }
}
