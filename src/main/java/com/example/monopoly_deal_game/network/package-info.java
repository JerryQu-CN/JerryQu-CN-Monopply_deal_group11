/**
 * Network layer — LAN multiplayer via TCP sockets with object serialization.
 * Host runs the authoritative game logic; clients send commands and receive
 * session snapshots.
 */
package com.example.monopoly_deal_game.network;