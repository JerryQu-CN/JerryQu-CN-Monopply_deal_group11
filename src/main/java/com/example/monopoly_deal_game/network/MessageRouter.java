package com.example.monopoly_deal_game.network;

/**
 * 将收到的字节/JSON 路由到具体处理（设计图 MessageRouter）。
 *
 * TODO(network): 定义消息枚举：JOIN、STATE_SYNC、PLAYER_CMD、ERROR等。
 */
public class MessageRouter {
    // TODO(network): register(handler)
}
