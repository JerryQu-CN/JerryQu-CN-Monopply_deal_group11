Package: com.example.monopoly_deal_game.network
================================================================================
[Included classes]
  - GameServer          Host listener, rooms and broadcast (skeleton).
  - NetworkClient       Client connection, send PlayerCommand, receive state (skeleton).
  - MessageRouter       Message type dispatch (skeleton).
  - ConnectionManager   Connection and reconnection (skeleton).
  - HeartbeatService    Heartbeat (skeleton, optional).
  (Also see package-info.java.)

[Interfaces]
  - None; can add MessageHandler and other interfaces as needed.

[What needs to be implemented]
  - Protocol definition: join room, state synchronization, disconnect handling.
  - Host authority: Clients must not directly modify model, only submit commands or display snapshots.
  - Integration with controller.SyncController (to be created).

[Relationships with other packages]
  - → logic: GameServer calls GameLogic to apply commands.
  - → model: Broadcast GameSession or its DTO.
  - uses logic.PlayerCommand as one of the upstream message payload types.
