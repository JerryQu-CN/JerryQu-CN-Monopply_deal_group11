package com.example.monopoly_deal_game.game.state;

/**
 * Target state enumeration for action state resolution — not targeted, targeted, refused, or accepted.
 */
public enum TargetState {
    NOT_TARGETED,
    TARGETED,
    REFUSED,
    ACCEPTED
}