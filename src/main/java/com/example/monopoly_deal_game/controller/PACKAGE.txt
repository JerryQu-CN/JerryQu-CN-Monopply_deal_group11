Package: com.example.monopoly_deal_game.controller
================================================================================
[Included classes]
  - ScreenNavigation          FXML paths, scene switching (width/height constants).
  - StageAware                Controller interface requiring a Stage reference.
  - StartScreenController     Main menu.
  - AddPlayerScreenController Human/room lobby (online placeholder).
  - AddBotPlayerScreenController Bot lobby (number of bots).
  - LoadGameScreenController  Load save path and validation.
  (Also see package-info.java.)

[Interfaces]
  - StageAware: setStage(Stage).

[What needs to be implemented]
  - Each button event connects with game.engine.GameEngine / GameLogic (start game, load save, online).
  - GameplayScreen.fxml: suggest adding a dedicated controller, binding hand/bank/property UI (Requirement 17).
  - Online: suggest adding SyncController to forward network messages and refresh the interface.

[Relationships with other packages]
  - → logic: Call use-case methods exposed by GameEngine; do not write settlement rules in this package.
  - → network: Lobby and game session synchronization (Room / Client).
  - → persistence: LoadGameScreen calls SaveGameService.
  - → view: Complex controls and GameObserver subscriptions can be placed in the view package, assembled by this package.
