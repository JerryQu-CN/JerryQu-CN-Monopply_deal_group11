Package: com.example.monopoly_deal_game.view
================================================================================
[Included classes / subpackages]
  - GameObserver, GameplayUiBundle, GameplayViewCoordinator (root package)
  - view.cards: AbstractCardNode and Property/Action/Bank card face subclasses, CardNodeFactory
  - view.animation: UiMotion, AbstractUiMotion, Shuffle/Draw/Place/MoveMotion, MotionContext
  - view.scene: UiSceneLayer, ScenePaneResolver (in-game layer -> Pane)
  (Also see each subpackage's package-info.java.)

[Interfaces]
  - GameObserver: onSessionUpdated(GameSession).

[What needs to be implemented]
  - Per Requirement 17, split custom controls: card Tooltip, player asset thumbnail area, central hand area, etc.
  - Concrete classes implementing GameObserver can be in this package, or implemented anonymously in controller.
  - Choose one: observer pattern or bidirectional binding to a read-only VM with JavaFX Property.

[Relationships with other packages]
  - → model: Read-only GameSession / DTO, do not directly modify the model.
  - ← logic: After engine state change, trigger onSessionUpdated (registered by controller or engine callback).
  - controller: FXML fx:controller remains in the controller package; this package holds reusable Node/controls.
