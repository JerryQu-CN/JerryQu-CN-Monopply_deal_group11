package com.example.monopoly_deal_game.model;

/**
 * 全局进行标记（设计图：currentPlayer、isGameRunning、roundCount 等）。
 *
 * TODO(model): 与 {@link GameSession} 分工——会话管牌堆与玩家列表，本类管「轮到谁、阶段、是否结束」。
 */
public class GameState {
    // TODO(model): 当前玩家索引、阶段枚举、是否暂停等待支付/Just Say No 链等
}
